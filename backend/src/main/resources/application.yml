spring:
  datasource:
    url: jdbc:h2:mem:a603
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
jwt:
  access:
    token:
      expiration:
        seconds: 3600
  refresh:
    token:
      expiration:
        seconds: 604800
  token:
    secret-key: itsepguyitsepguyitsepgyitsitsits